# LLM providers — JSON array, first entry = primary, fallback in order on failure
# Each provider must expose an OpenAI-compatible chat completions API
LLM_PROVIDERS=[{"name":"kimi","baseUrl":"https://api.moonshot.cn/v1","apiKey":"sk-xxx","model":"moonshot-v1-32k"},{"name":"gemini","baseUrl":"https://generativelanguage.googleapis.com/v1beta/openai/","apiKey":"xxx","model":"gemini-1.5-flash"}]

# TTS — Murf API key (get from murf.ai)
MURF_API_KEY=your_murf_api_key_here

PORT=3001
